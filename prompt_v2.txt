Here is my current Alloy model. I want you to act as an expert in formal verification and help me debug it using my local MCP server tools.

Please follow this exact order of operations:

1. **Syntax First:** Use the `check_alloy_syntax` tool to parse the model. Do not attempt to fix logic or run the solver until the syntax passes completely.

2. **Execution:** Once the syntax is valid, use the `run_alloy_model` tool to execute my target command. 
   * **Dynamic Limits:** To protect your context window, please set `max_return` to 5 (so you only see the first 5 traces immediately), but set `max_archive` to 200 so the backend saves everything to disk just in case we need it later.

3. **CRITICAL NOTE ON TIMEOUTS:** The backend SAT solver is calculating and archiving counterexamples to a local hard drive. For complex temporal models, this takes time. If the tool returns a message starting with **"⏳ The SAT solver is taking longer than 45 seconds,"** DO NOT PANIC and DO NOT try to run it again. This simply means the process successfully detached and is running safely in the background. Acknowledge that the background job has started, and wait for me to tell you to use the `read_archived_counterexamples` tool.

4. **IF EXECUTION IS FAST:** If the tool completes quickly and successfully returns the first few counterexamples directly in the chat, review the provided solutions and give me:
   * A summary of the assertion failure or predicate inconsistency.
   * A comparative breakdown of the solutions you can see. Explain exactly how the failure states differ from each other structurally or temporally.
   * An explanation of the root logical flaw in my code.
   * A concrete code recommendation to fix the issue.

**Target Command:** [Insert command name here, e.g., "CheckSafety", or leave blank for "all"]
**Here is the code:**
[Insert Your Alloy Code Here]